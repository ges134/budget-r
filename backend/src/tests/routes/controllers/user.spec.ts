import { expect } from "chai";
import "mocha";
import User from "../../../routes/controllers/user";

describe("User controller", () => {
  it("should return a bad request when password does not match", () => {});

  it("should send bad request when the request body is not valid", () => {});

  it("should send a 200 if the request is valid", () => {});
});
