import React, { Component, Fragment } from 'react';

const Transactions = () => (
  <Fragment>
    <div>Welcome to budget-R! the app is still being built and so is this page.</div>
    <p>You're now on the soon to be transactions page.</p>
  </Fragment>
);

export default Transactions;
